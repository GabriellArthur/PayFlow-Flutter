name: boletos
description: A new Flutter project.

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2  
  #fonts
  google_fonts: ^2.1.0
  #reconhecer o login
  firebase_core: ^1.6.0
  #login com o google
  google_sign_in: ^5.0.4
    #rodando no web
  google_sign_in_web: ^0.10.0+3
  #Salvar arquivo 
  shared_preferences: ^2.0.6
  #Camera e Arquivo
  google_ml_kit: ^0.6.0 #Ler a camera
  camera: ^0.8.1+3
  image_picker: ^0.8.0+4
    #rodando no web
  image_picker_for_web: ^2.0.0
  #Fonte
  font_awesome_flutter: ^9.1.0
  flutter_masked_text2: ^0.9.1
  #Animacao
  animated_card: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:

  uses-material-design: true

  assets:
    - assets/images/
